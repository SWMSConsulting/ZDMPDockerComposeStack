{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ .Values.udaBackendWebApi.service.name }}-ingress
  namespace: {{ .Release.Namespace }}
  annotations:
    # use the shared ingress-nginx
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/proxy-body-size: 100m
spec:
  rules:
  - host: {{ .Values.udaBackendWebApi.service.name }}-zdmp.{{ .Values.ingress.domain }}
    http:
      paths:
      - path: /
        backend:
          serviceName: {{ .Values.udaBackendWebApi.service.name }}
          servicePort: {{ .Values.udaBackendWebApi.ports.port }}
  tls:
  - hosts:
    - {{ .Values.udaBackendWebApi.service.name }}-zdmp.{{ .Values.ingress.domain }}
{{- end }}
